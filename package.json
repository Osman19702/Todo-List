{
  "name": "TodoList",
  "version": "1.0.0",
  "description": "Todo List desktopp app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --icon=assets/icons/mac/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . Todo List --overwrite --platform=win32 --arch=ia32 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Todo List\"",    
    "package-linux": "electron-packager . Todo List --overwrite --asar --platform=linux --arch=x64 --icon=assets/icons/png/icon.png --prune=true --out=release-builds"
  },
  "author": "Osman T",
  "license": "MIT",
  "dependencies": {
    "electron": "^18.3.7"
  },
  "devDependencies": {
    "electron-packager": "^14.0.1"
  }
}
